{% extends 'admin/base.html.twig' %}

{% block title %}EDITION D'UNE CATEGORIE{% endblock %}

{% block body %}

<div class="container col-6 mt-5 mb-4 alert alert-light rounded-lg">
    <h1 class="mb-5 text-center">MODIFIER UNE CATEGORIE</h1>

    {{form_start(form)}}
    

    <div class="row">
        <div class="col-6">   
            {{ form_row(form.name) }}
        </div>
        <div class="col-6"> 
            {{ form_row(form.color) }}
        </div>
    </div>
    <div class="mb-4">  
        {{ form_row(form.description) }}
    </div>
       
 
    {{ form_end(form) }}

</div>
{% endblock %}