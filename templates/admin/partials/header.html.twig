{# Barre de navigation admin#}

{% block stylesheets %}
    <link href="https://fonts.googleapis.com/css2?family=Permanent+Marker&display=swap" rel="stylesheet">
{% endblock %}

        <nav class="navbar navbar-expand-lg navbar-dark bg-info sticky-top">
            <a class="navbar-brand" href="{{ path('homepage') }}"><img src="/img/logo-loc-appart3.png" alt="logo Loc'Appart" class="logo-mini p-2"></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_dashboard') }}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;">
                            <i class="fas fa-chart-line"></i>
                            Dashboard
                        </a>
                    </li>
                    {# <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_ads_index') }}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;">
                        <i class="fas fa-igloo"></i>
                            Annonces
                        </a>
                    </li> #}
                    <li class="nav-item dropdown">
                        <a href="#" id="accountDropdownLink" class="nav-link dropdown-toggle " style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;" data-toggle="dropdown" id="acccountDropdownLink"><i class="fas fa-comments" aria-hidden="true"></i> Annonces</a>
                        <div class="dropdown-menu dropdown-menu-right bg-info text-light" style="width: 12vw;" aria-labelledby="accountDropdownLink">
                            <a class="nav-link" href="{{path('admin_ads_index')}}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;"><i class="fas fa-align-justify"></i><b> Publiées <span class="bg-warning text-dark ml-3 p-1 rounded-lg rounded-circle">{{ stats.validatedAd }}</span></b></a>
                            <a class="nav-link" href="{{ path('admin_ads_toValidate') }}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;"><i class="fas fa-check-circle"></i> <b> A valider <span class="bg-warning text-dark ml-3 p-1 rounded-circle">{{ stats.waitingAd }}</span></b></a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_booking_index') }}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;">
                            <i class="far fa-calendar-alt"></i>
                            Réservations
                        </a>
                    </li>
                    {# <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_comment_index')}}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;">
                            <i class="fas fa-comments"></i>
                            Commentaires
                        </a>
                    </li> #}
                    <li class="nav-item dropdown">
                        <a href="#" id="accountDropdownLink" class="nav-link dropdown-toggle " style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;" data-toggle="dropdown" id="acccountDropdownLink"><i class="fas fa-comments" aria-hidden="true"></i> Commentaires</a>
                        <div class="dropdown-menu dropdown-menu-right bg-info text-light" style="width: 12vw;" style="width: 15vw;" aria-labelledby="accountDropdownLink">
                            <a class="nav-link" href="{{path('admin_comment_index')}}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;"><i class="fas fa-align-justify"></i><b> Publiés <span class="bg-warning text-dark ml-3 p-1 rounded-lg rounded-circle">{{ stats.validatedComment }}</span></b></a>
                            <a class="nav-link" href="{{ path('admin_comment_toValidate') }}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;"><i class="fas fa-check-circle"></i> <b> A valider <span class="bg-warning text-dark ml-3  p-1 rounded-circle">{{ stats.waitingComment }}</span></b></a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_category_index')}}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;">
                            <i class="fa fa-th-large"></i>
                            Catégories
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_role_index')}}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;">
                            <i class="fas fa-user-plus"></i>
                            Rôles
                        </a>
                    </li>
                    {# <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_user_index')}}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;">
                            <i class="fas fa-users"></i>  
                            Utilisateurs
                        </a>
                    </li> #}
                    <li class="nav-item dropdown">
                        <a href="#" id="accountDropdownLink" class="nav-link dropdown-toggle " style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;" data-toggle="dropdown" id="acccountDropdownLink"><i class="fas fa-comments" aria-hidden="true"></i> Utilisateurs</a>
                        <div class="dropdown-menu dropdown-menu-right bg-info text-light" style="width: 15vw;" aria-labelledby="accountDropdownLink">
                            <a class="nav-link" href="{{path('admin_user_index')}}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;"><i class="fas fa-chart-bar"></i><b> Profil d'activité <span class="bg-warning text-dark ml-3 p-1 rounded-lg rounded-circle">{{ stats.validatedUser }}</span></b></a>
                            <a class="nav-link" href="{{ path('admin_user_details') }}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;"><i class="fas fa-address-card"></i> <b> Profil détaillé <span class="bg-warning text-dark ml-3 p-1 rounded-circle">{{ stats.validatedUser }}</span></b></a>
                            <a class="nav-link" href="{{ path('admin_user_details_toValidate') }}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;"><i class="fas fa-check-circle"></i> <b> Profil à valider <span class="bg-warning text-dark ml-3 p-1 rounded-circle">{{ stats.waitingUser }}</span></b></a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('admin_carousel_index')}}" style="font-size: 1.2rem; font-family: 'Permanent Marker', cursive;">
                            <i class="fas fa-images"></i>
                            Carousel
                        </a>
                    </li>
                </ul>
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item dropdown">
                        <a href="#" id="accountDropdownLink" class="nav-link dropdown-toggle" data-toggle="dropdown">
                            <img src="{{app.user.picture}}" alt="Avatar de {{app.user.fullName}}" class="avatar avatar-header border border-light"> <b>{{app.user.fullName}}</b>
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                            <a href="{{path('homepage')}}" target="_blank" class="dropdown-item text-dark font-weight-bold"><i class="fas fa-undo-alt mr-2"></i> Retour au site</a>
                            <div class="dropdown-divider"></div>
                            <a href="{{path('account_register')}}" class="dropdown-item text-dark font-weight-bold"><i class="fas fa-sign-out-alt mr-2"></i> Déconnexion</a>
                        </div>
                    </li>
                    
                </ul>
            </div>
        </nav>

        {% for label, messages in app.flashes %}

        <div class="container">
            <div class="alert alert-{{label}}">
                {% for message in messages %}
                    <p>{{message | raw}}</p>
                {% endfor %}
            </div>
        </div>
            
        {% endfor %}